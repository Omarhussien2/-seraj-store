---
phase: 07-fas7a-helwa-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/kidzapp-types.ts
  - scripts/scrape-kidzapp-egypt.ts
  - scripts/parks-gardens-egypt.ts
  - data/.gitkeep
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "All 615 Kidzapp Egypt experiences fetched with retry error handling"
    - "CSV matches PlaceRow schema with 22 fields"
    - "Every row shows data source and real price date from API"
    - "Egyptian public parks & gardens compiled as separate dataset"
    - "data/ directory exists, CSV files gitignored"
    - "Merged deduplicated CSV ready for MongoDB seeding"
  artifacts:
    - path: "scripts/kidzapp-types.ts"
      provides: "Shared TypeScript types for API + CSV + future backend model"
    - path: "scripts/scrape-kidzapp-egypt.ts"
      provides: "Kidzapp scraper with pagination + retry + error handling"
      min_lines: 100
    - path: "scripts/parks-gardens-egypt.ts"
      provides: "Hardcoded parks + merge script"
    - path: "data/fas7a-helwa-merged.csv"
      provides: "Final 450+ places CSV (gitignored)"
  key_links:
    - from: "scripts/scrape-kidzapp-egypt.ts"
      to: "api.kidzapp.com/api/3.0/experiences/"
      via: "fetch with pagination + retry"
      pattern: "fetch.*kidzapp"
---

<objective>
Data extraction pipeline: Kidzapp API (615 venues) + Egyptian parks/gardens → clean merged CSV ready for MongoDB seeding in Plan 02.

User Decisions (LOCKED):
1. English names fine — translate later
2. Real price dates (2019) shown for trust
3. Public parks & gardens included across Egypt
4. Source attribution on every row
5. Simple { lat, lon } — NOT GeoJSON, no 2dsphere for MVP
6. No slug field — MongoDB _id used for routes
</objective>

<execution_context>
@C:/Users/omarh/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/omarh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-fas7a-helwa-data/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup data/ + Build Kidzapp Scraper with Error Handling</name>
  <files>data/.gitkeep, .gitignore, scripts/kidzapp-types.ts, scripts/scrape-kidzapp-egypt.ts</files>
  <action>
1. Create data/ directory: `mkdir -p data` then create empty `data/.gitkeep`
2. Append to `.gitignore`:
   ```
   # Data files (generated CSV — too large for git)
   data/*.csv
   ```
3. Install: `npm install --save-dev json2csv`

4. Create `scripts/kidzapp-types.ts` — shared types for scraper AND Plan 02 backend:
   - `KidzappExperience` — full API shape (id, uid, name, title, title_en, description, address, location:{lat,lon}, phone, website, instagram_link, image_url, image_carousel_list, venue_type[], price_range, booking_required, ages_display[], working_hours[], categories[], primary_category, area:{id,name_en}, city:{id,name_en,name_ar}, price[]{id,type_en,type_ar,final_price,currency}, price_age_groups, price_age_prices, created_at, url)
   - `KidzappPaginatedResponse` — { count, next: string|null, previous: string|null, results[] }
   - `PlaceRow` — unified schema matching CSV + future MongoDB model:
     * id, name_ar, name_en, description_short, area, city, address
     * lat: number, lon: number (SIMPLE — not GeoJSON)
     * min_price, max_price, price_range_id (1-5), min_age, max_age, avg_duration_hours
     * is_free: boolean, indoor_outdoor: "indoor"|"outdoor"|"mixed"|"unknown"
     * booking_required: boolean
     * website_url, external_source, external_detail_url, phone, facebook_url, instagram_url
     * category_ids: string (semicolon-separated), image_url, last_price_update: string
   - `VENUE_TYPE_MAP`: ["indoors"]→"indoor", ["outdoors"]→"outdoor", both→"mixed", []→"unknown"
   - `CATEGORY_MAP`: {66517:1, 66514:2, 66518:3, 66512:4, 37:5, 66513:1, 38:3, 49:3, 45:1, 48:3, 66516:6}
   - `LOCAL_CATEGORIES`: {1:"Fun & Play", 2:"Shows & Cinema", 3:"Outdoor & Parks", 4:"Art & Educational", 5:"Farms & Animals", 6:"Eat Out"}
   - Export all types for reuse in Plan 02

5. Create `scripts/scrape-kidzapp-egypt.ts`:
   - Native fetch() to `https://api.kidzapp.com/api/3.0/experiences/?country_code=eg&page_size=100`
   - **Error handling — retry**: wrap fetch in try/catch, retry up to 3 times with 1s delay, log which page failed. If permanent failure, save what we have and continue.
   - **Error handling — schema resilience**: wrap each experience transform in try/catch. If one row fails (missing/unexpected field), log row ID + error, skip it, don't crash. Track skipped count in summary.
   - Pagination: follow data.next links (7 pages, 200ms delay between pages)
   - Filter: type === "venue" AND show === true
   - Transform to PlaceRow:
     * city: exp.city?.name_en || ''
     * lat/lon: exp.location?.lat || 0, exp.location?.lon || 0 (SIMPLE)
     * min_price/max_price: from price[].final_price > 0, or parse price_age_prices text
     * last_price_update: exp.created_at.split('T')[0] (2019 dates = TRUST)
     * external_source: "Kidzapp"
     * external_detail_url: "https://kidzapp.com/" + exp.url
   - json2csv → CSV with BOM prefix → `data/fas7a-helwa-kidzapp.csv`
   - Print summary: total fetched, venues, skipped rows, city/category breakdown

6. Run: `npx tsx scripts/scrape-kidzapp-egypt.ts`
  </action>
  <verify>Console: "Fetched 615, 400+ venues, 0 skipped". data/ exists. CSV has correct header. git status shows CSV gitignored but .gitkeep tracked.</verify>
  <done>400+ Kidzapp venues in CSV with error handling, data/ dir setup, CSV gitignored</done>
</task>

<task type="auto">
  <name>Task 2: Compile Parks & Gardens + Merge All Sources</name>
  <files>scripts/parks-gardens-egypt.ts, data/fas7a-helwa-parks-gardens.csv, data/fas7a-helwa-merged.csv</files>
  <action>
1. Create `scripts/parks-gardens-egypt.ts` importing PlaceRow from kidzapp-types.ts:

   Hardcode 25+ Egyptian parks/gardens with ARABIC names:

   **Cairo:** Al-Azhar Park (حديقة الأزهر, Salah Salem, EGP 20-40), Al-Andalus Garden (حديقة الأندلس, Zamalek, Free), Japanese Garden (الحديقة اليابانية, Helwan, EGP 5), Orman Botanical (حديقة الأورمان, Giza, EGP 10), Giza Zoo (حديقة حيوان الجيزة, Giza, EGP 5-10), Fish Garden (حديقة الأسماك, Zamalek, EGP 5), Al-Horreya Garden (حديقة الحرية, Heliopolis, Free), Family Park (حديقة العائلة, Madinaty, Free), CFC Park (New Cairo, Free), International Park (حديقة الدولية, Nasr City, EGP 10), Merryland Park (Cairo, EGP 10), El Kobba Gardens (Cairo, EGP 5)

   **Alexandria:** Montazah Gardens (حدائق المنتزة, Free), Antoniadis Garden (حديقة أنطونياديس, EGP 10), Alex Zoo (حديقة حيوان الإسكندرية, EGP 5), Stanley Bridge (Free), Shallalat Gardens (حدائق الشلالات, Free)

   **Other:** Aswan Botanical (حديقة أسوان النباتية, EGP 20), Wadi Elrayan (Fayoum, EGP 20), Wadi El Hitan (Fayoum, EGP 50), Ras Muhammad (Sharm, EGP 100), Colored Canyon (Dahab, Free), Blue Hole (Dahab, Free), Fayoum Waterfalls (Free)

   Each: external_source="Manual", category_ids=3 or 5 (zoos), indoor_outdoor="outdoor", booking_required=false, last_price_update="2024-01-01", city populated, lat/lon=0.

2. Write parks CSV → data/fas7a-helwa-parks-gardens.csv

3. Merge (same script):
   - Read both CSVs with proper quoted-field parser (descriptions contain commas)
   - Deduplicate by name_en (case-insensitive trim)
   - Re-number id sequentially
   - Write merged → data/fas7a-helwa-merged.csv

4. Print summary: total, city breakdown, source breakdown, free count, stale-price count
  </action>
  <verify>data/fas7a-helwa-parks-gardens.csv: 25+ rows with Arabic name_ar.
   data/fas7a-helwa-merged.csv: 450+ unique rows, no duplicates, every row has external_source.</verify>
  <done>Merged deduplicated CSV 450+ places ready for MongoDB seeding in Plan 02</done>
</task>

</tasks>

<verification>
1. data/ dir exists, CSV files gitignored, .gitkeep tracked
2. Scraper fetches 615, handles errors gracefully, 400+ venues in CSV
3. Parks script: 25+ parks + merged file
4. data/fas7a-helwa-merged.csv: 450+ rows, no duplicates, BOM prefix
</verification>

<success_criteria>
- 450+ unique places in merged CSV
- Scraper has retry + skip-bad-row error handling
- data/ dir with gitignore for CSV files
- PlaceRow type exported for Plan 02 backend
</success_criteria>

<output>
After completion, create `.planning/phases/07-fas7a-helwa-data/07-01-SUMMARY.md`
</output>
